node_modules/
dist/
.wrangler/

# Solana build artifacts
target/
*.so

# Keypair files — NEVER commit private keys
*.json
!package.json
!pnpm-lock.yaml
!tsconfig*.json

# Processor keypair specifically (belt + suspenders)
processor-keypair.json
processor-keypair-*.json
*-keypair.json

# Environment files
.env
.env.local
.dev.vars

# Editor
.DS_Store
*.swp
